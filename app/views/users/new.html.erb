<%= form_for(@user) do |form| %>
	<div class="field">
		<%= form.label :username %>
		<%= form.text_field :username, autocapitalize: 'off' %>
	</div>
	<div class="field">
		<%= form.label :password %>
		<%= form.password_field :password %>
	</div>
	<%= form.submit "Sign up" %>
<% end %>